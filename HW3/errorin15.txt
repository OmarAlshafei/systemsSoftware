const a = 2, b = 3;
var x, y, z;
begin
  /*arithmetic equations must contain operands, parentheses, numbers, or symbols*/
  x := a + 5;
  y := a * (x - b);
  z := (b + y * x) / 10;
  read z;
  if y <> x then 
    write y;
  xor odd y + z then 
    z := y; 
  else
    y := z;
  while z <  do
    z := z + 1;
end.